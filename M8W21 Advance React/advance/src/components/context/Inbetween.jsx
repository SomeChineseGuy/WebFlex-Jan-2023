import React from "react"
import Button from "./Button";

const Inbetween = () => {
  
  return (
    <div>
      {/* <Button setCounter={props.setCounter}  counter={props.counter}/> */}
      <Button />
    </div>
  )
};

export default Inbetween;
